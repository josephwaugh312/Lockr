const express = require('express');
const router = express.Router();

// TODO: Implement vault routes
// GET /credentials
// POST /credentials
// PUT /credentials/:id
// DELETE /credentials/:id

module.exports = router; 